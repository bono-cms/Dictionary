<?php

use Krystal\Form\Element;
use Krystal\Grid\Grid;
use Cms\View\Icon;

defined('KRYSTAL') or die;

?>

<div class="btn-toolbar">
	<div class="btn-group">
	  <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
		<i class="glyphicon glyphicon-list"></i> <?php $this->show('Menu'); ?> <span class="caret"></span>
	  </button>
	  <ul class="dropdown-menu">
		 <li><a href="<?php echo $this->url('Dictionary:Admin:Dictionary@addAction'); ?>"><i class="glyphicon glyphicon-file"></i> <?php $this->show('Add new item'); ?></a></li>
		 <li class="divider"></li>
		 <li class="dropdown-header"><?php $this->show('Actions'); ?></li>
		 <?php if (!empty($items)): ?>
		 <li><a href="#" data-button="remove-selected" data-url="<?php echo $this->url('Dictionary:Admin:Dictionary@deleteAction', null); ?>"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a></li>
		 <?php else: ?>
		 <li class="disabled"><a><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Save changes'); ?></a></li>
		 <li class="disabled"><a><i class="glyphicon glyphicon-remove"></i> <?php $this->show('Remove selected'); ?></a></li>
		 <?php endif; ?>
	  </ul>
	</div>
</div>

<h2 class="page-header"><i class="glyphicon glyphicon-file"></i> <?php $this->show('Dictionary'); ?> <small>(<?= count($items); ?>)</small></h2>

<form>
	<div class="table-responsive">
        <?= Grid::render($items, array(
            'batch' => true,
            'pk' => 'id',
            'tableClass' => 'table table-hover table-bordered table-striped table-condensed',
            'columns' => array(
                array(
                    'column' => 'id',
                    'label' => '#'
                ),

                array(
                    'column' => 'value',
                ),

                array(
                    'column' => 'alias'
                )
            ),

            'actions' => array(
                'edit' => function($row){
                    return Icon::edit(
                        $this->url('Dictionary:Admin:Dictionary@editAction', $row['id']),
                        $this->translate('Edit this item')
                    );
                },

                'remove' => function($row){
                    return Icon::remove(
                        $this->url('Dictionary:Admin:Dictionary@deleteAction', $row['id']),
                        $this->translate('Remove this item')
                    );
                }
            )

        ), $this->translator); ?>
        
		<?php if (empty($items)): ?>
		<div class="center-block">
			<h2 class="text-center text-muted"><i class="glyphicon glyphicon-remove"></i> <?php $this->show('No items'); ?></h2>
		</div>
		<?php endif; ?>
	</div>
</form>

<?php $this->loadPartial('paginator'); ?>
