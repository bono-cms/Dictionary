<?php

use Krystal\Form\Element;
use Cms\Service\LanguageManager;

defined('KRYSTAL') or die;

?>

<form data-group="contact">
    <ul class="nav nav-tabs" role="tablist">
        <li role="presentation" class="active"><a href="#dictionary" role="tab" data-toggle="tab"><i class="glyphicon glyphicon-file"></i> <?php $this->show('Dictionary'); ?></a></li>
        <li role="presentation"><a href="#configuration" role="tab" data-toggle="tab"><i class="glyphicon glyphicon-th-list"></i> <?php $this->show('Configuration'); ?></a></li>
	</ul>
    
    <div class="tab-content">
        <div class="tab-pane fade in active" id="dictionary" role="tabpanel">
            <div class="well">
                <?php $this->loadPartial('language-tabs'); ?>

                <div class="tab-content">
                    <?php foreach ($languages as $index => $language): ?>
                    <div class="tab-pane fade in <?php echo $index == 0 ? 'active' : null; ?>" id="<?php printf('language-%s', $language->getId()); ?>" role="tabpanel">
                        <?php $entity = LanguageManager::findByLangId($language->getId(), $item); ?>

                        <?php echo Element::hidden(sprintf('translation[%s][id]', $language->getId()), $entity->getId()); ?>
                        <?php echo Element::hidden(sprintf('translation[%s][lang_id]', $language->getId()), $language->getId()); ?>

                        <?php $this->loadPartial('language-name', array('language' => $language)); ?>

                        <div class="form-group">
                            <label class="col-lg-2 control-label"><?php $this->show('Value'); ?></label>
                            <div class="col-lg-10">
                                <div class="input-group">
                                    <?php echo Element::textarea(sprintf('translation[%s][value]', $language->getId()), $entity->getValue(), array('rows' => 3, 'class' => 'form-control', 'placeholder' => $this->translate('Name'))); ?>
                                    <span class="input-group-addon"><i class="glyphicon glyphicon-pencil"></i></span>
                                </div>
                            </div>
                        </div>
                    </div>
                    <?php endforeach; ?>
                </div>
            </div>
        </div>

        <div class="tab-pane fade" id="configuration" role="tabpanel">
            <div class="well">
                <?php echo Element::hidden('item[id]', $entity->getId()); ?>

                <div class="form-group">
                    <label class="col-lg-2 control-label"><?php $this->show('Alias'); ?></label>
                    <div class="col-lg-10">
                        <?php echo Element::text('item[alias]', $entity->getAlias(), array('class' => 'form-control', 'placeholder' => $this->translate('Alias'))); ?>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <?php if (!$entity->getId()): ?>
    <button data-button="add" data-back-url="<?php echo $this->url('Dictionary:Admin:Dictionary@editAction', null); ?>" data-url="<?php echo $this->url('Dictionary:Admin:Dictionary@saveAction'); ?>" class="btn btn-primary"><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Add'); ?></button>
	<button data-button="add-create" data-url="<?php echo $this->url('Dictionary:Admin:Dictionary@saveAction'); ?>" class="btn btn-primary"><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Add & Create new'); ?></button>

    <?php else: ?>
	<button data-button="save" data-url="<?php echo $this->url('Dictionary:Admin:Dictionary@saveAction'); ?>" class="btn btn-primary"><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Save'); ?></button>
	<button data-button="save-create" data-back-url="<?php echo $this->url('Dictionary:Admin:Dictionary@addAction'); ?>" data-url="<?php echo $this->url('Dictionary:Admin:Dictionary@saveAction'); ?>" class="btn btn-primary"><i class="glyphicon glyphicon-floppy-save"></i> <?php $this->show('Save & Create new'); ?></button>
	<?php endif; ?>

	<button data-button="cancel" data-url="<?php echo $this->url('Dictionary:Admin:Dictionary@indexAction'); ?>" class="btn btn-default"><i class="glyphicon glyphicon-floppy-remove"></i> <?php $this->show('Cancel'); ?></button>
</form>
